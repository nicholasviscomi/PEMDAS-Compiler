package main

import (
	"fmt"
)

//use a stack and maybe a lexer to compute
//basic math operations
// 5 + 8 * 7 / (5 - 2)

func main() {
	for true {
		fmt.Print("BASIC >> ")
		var input string
		fmt.Scanln(&input)
	}
}